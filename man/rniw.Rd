% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rniw}
\alias{rniw}
\title{Sample from a Normal-Inverse-Wishart distribution}
\usage{
rniw(n, mean, Psi, kappa, nu)
}
\arguments{
\item{n}{Integer. Number of independent draws.}

\item{mean}{Numeric vector of length \eqn{p}: the prior mean \eqn{\xi}.}

\item{Psi}{Symmetric positive-definite \eqn{p \times p} matrix: scale matrix \eqn{\Psi}.}

\item{kappa}{Positive scalar: prior precision multiplier on the mean.}

\item{nu}{Degrees of freedom; must satisfy \code{nu > p - 1}.}
}
\value{
A list with two elements:
\describe{
\item{mu}{A \eqn{p \times n} matrix; column \code{i} is the i-th \eqn{\mu} draw.}
\item{Sigma}{A \eqn{p \times p \times n} array; slice \code{[, , i]} is the i-th \eqn{\Sigma} draw.}
}
}
\description{
Draws \code{n} independent samples \eqn{(\mu, \Sigma)} from a
Normal-Inverse-Wishart distribution:
\deqn{\Sigma \sim \mathcal{IW}(\Psi, \nu), \qquad
      \mu \mid \Sigma \sim \mathcal{N}(\xi,\; \Sigma / \kappa).}
}
\examples{
p   <- 3
sim <- 10000
Psi <- diag(p); Psi[1,2] <- Psi[2,1] <- -.5
out <- rniw(n = sim, mean = rep(0, p), Psi = Psi, kappa = 1, nu = p + 5)

dim(out$mu)                       # p x n
dim(out$Sigma)                    # p x p x n
rowMeans(out$mu)                  # ~ ex
apply(out$Sigma,c(1,2),sum)*4/sim # ~ Psi

}
