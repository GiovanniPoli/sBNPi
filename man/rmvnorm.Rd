% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rmvnorm}
\alias{rmvnorm}
\title{Sample from a multivariate Normal distribution}
\usage{
rmvnorm(n, mu, sigma)
}
\arguments{
\item{n}{Integer. Number of independent draws.}

\item{mu}{Numeric vector of length \eqn{p}: the mean.}

\item{sigma}{Symmetric positive-definite \eqn{p \times p} covariance matrix.}
}
\value{
A \eqn{p \times n} numeric matrix; column \code{i} is the i-th draw.
}
\description{
Draws \code{n} independent samples from \eqn{\mathcal{N}(\mu, \Sigma)}
by computing the Cholesky factor of \eqn{\Sigma} once and applying it to
a \eqn{p \times n} matrix of standard normals.
}
\examples{
p     <- 3
mu    <- c(1, -1, 2)
sigma <- diag(p)
X <- rmvnorm(n = 5000, mu = mu, sigma = sigma)
rowMeans(X)         # ~ mu
cov(t(X))           # ~ sigma

}
